import React from 'react';
import ReactDOM from 'react-dom';

import App from './App'

ReactDOM.render(<App />, document.querySelector('#root'));
// everything created in react is going to be inside of the App and put it inside of the root


